--- 
TODO
check ui clean up 

TODO
check recent in codex chats and go throught them last 6ish

TODO
view the claude with rules etc 

TODO
view claude for speed data updates

TODO sidebar
Deeply investigate and fix why the authenticated staff sidebar remains permanently in `SidebarNavSkeleton` even though `/api/shell/bootstrap` returns a valid `sidebarAccess` list, trace the full user, session, role, workspace access and sidebar resolution flow from authentication through the relevant providers, hooks, manifest logic and StaffLayout conditions, determine exactly which state never reaches resolved and why, compare normal Keycloak login behaviour with any dev-login or local authentication path, investigate the relationship with the existing `src/config/workspace/manifest.test.js` nav/access parity failures, check for role-shape mismatches, stale or duplicated state, provider ordering issues, race conditions, hydration differences, failed role mapping or access calculations, confirm if the unresolved state affects anything beyond sidebar rendering such as page permissions, route visibility or page data loading, fix the root cause without weakening RBAC, bypassing permission checks or hardcoding Admin access, and once fixed verify the sidebar fully resolves for representative Admin, Manager, Workshop, Parts and Sales role states in both light and dark mode.


TODO performance
Take the next HNPSystem performance pass specifically on /tech/[jobNumber], which is now the heaviest important route at about 1.92 MB because it statically imports the 13,287-line job-card page to reuse three components, carry out the previously measured extraction of the required technician-facing job-card functionality into reusable shared components without importing the full job-card page, use the identified 22-symbol roughly 3,533-line dependency surface as the starting point but revalidate the exact dependency boundary against the current tree before moving anything, preserve the technician default Customer Requests workflow, VHC behaviour, clocking, permissions, state handling, styling, modal behaviour and all existing staffglobal.css integration exactly, keep primary-workflow controls such as the camera and customer-video buttons immediately available rather than deferring them merely to save bytes, update any generated development source mapping or governance metadata affected by the extraction, avoid duplicating business logic between the technician and job-card routes, measure the technician route before and after with the current bundle diagnostics and hnpPerf tooling and aim to remove the large job-card dependency so the route approaches the previously estimated roughly 1.1 MB first-load size, retain all Supabase dynamic-import, realtime coalescing, shell bootstrap, lhr1 and previous performance changes, and do not broaden the work into unrelated routes unless the extraction exposes a shared dependency whose removal produces a clear measured benefit without changing behaviour.


---

CHECK
have it so within the "Inspection" section within the customer video recording section this is to make sure the text doesnt clash with the background colour, atm when in light mode the ligth text is clashing etc 

CHECK 
wihtin the job tracker sidebar section 


CHECK  
Deep dive into the job tracker sidebar seciton with the scroll and left side lines conneting the sections



TODO
Within "https://hnpsystem.vercel.app/vhc/customer/00076/OSD7Ds_cq_77" for example page remove the notes button, this isn’t visible. Have it so the buttons authorise and decline are set to custglobal.css. Have it so the wheels and tires and brakes and hubs section shows the measurement as well as all of them showing the notes of the concern within the rows. Add a table bottom line in between the rows.  In the top right of this page add a profile button which takes the user to the website/login page or the website/profile page depending if the user is already logged in. Have it so this page is linked to the customers profile page as well so they can open it when they wish. Make sure this page follows the custglobal.CSS page fully. 

TODO
Improve the layout of the website/profile page, I feel like it it’s really long page and not very easy to navigate. Have it so alll /website pages works the best with phone vertical mode. as the main screen size to use. and then secondary for larger screens etc. 

TOD with one above maybe. 
Depending on the layout change, have it so the JUMP TO section is hidden in a menu button instead of always visible. 

TODO
chgeck buttons, not following shape and style is this becuase they arent following staffglobal.css settings deep dive into this across all pages

TODO
UPDATE GLOBAL SETTINGS BASED ON THE /staff/website-manager PAGE OUTCOME wiht 




- /website/profile (CHECK)
check global.css style
have it detect if the reg numbers already on the system

UPDATE GLOBAL SETTINGS BASED ON THE /staff/website-manager PAGE OUTCOME wiht dropdown, tab section etc

add a see preview button which when pressed opens a new webbrowser tab and shows what the changes are making from the /staff/website-manager page in real time etc. delete image but still showing fix this as well

- /website (CHECK)
add a link which the staff uers can add delete uploa change etc anything within the /website page from a website page accessed within the "/login" section




--------------------- Cust booking ------------------------
DO it so customer books job thorugh, see if it updates the stuff section. 

have it os the appoinment page is updated within the /website/profile page

i dont see the customer status within the "/job-cards/00076 :: 1.2.1.1.1.2 (jobcard-summary-shell)" section 

CHECK
Have it so within the presnentaion when overview or Business Value popup is highlighting a section, have it so it scrolls the page and makes sure the highlighted section can is within the view of the screen.


CEHCK
customer dev login so it hsows the customers created within the app and shows all the correct details.

CEHCK
monthly pcikers within the app to use global.css style



TODO
look into "Review Media" section after editing photo



TODO DEEP ONE
with camera for concerns, hgave ir link rto vhc photo and vedio section  in catagory of wipers etc

CHECK
Mileage to show within the customer page inside previous job section. 


TODO
npm i next@latest