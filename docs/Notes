Promote only these finished commits from development into main: [PASTE COMMIT HASHES OR COMMIT NAMES HERE], do not include any other development commits, preserve all unfinished development work exactly as it is, push the updated main branch to origin, then switch the local workspace back to development and confirm development remains the active branch.


Status style section!
.app-summary-item


CHECK
Cancel appointment button to be added within the jobcard scheduling tab button etc. 


Manager update give url link


TODO 
/Clocking
Minor changes todo 



z
TODO
/consumables-tracker
Minor changes todo 







TODO
/tech/efficiency
Update the `/tech/efficiency` page within the existing `App Layout Page Card` so it becomes a compact technician productivity and efficiency workspace while preserving all current technician and MOT access rules, technician-profile scoping, existing `tech_efficiency_entries`, `tech_efficiency_targets`, `job_clocking` and overtime data relationships, existing realtime refresh behaviour, manual-entry permissions, PDF export and efficiency calculations, inspect the repository and reuse the current shared components, APIs, database fields, target logic, job-request allocations and clocking-derived records rather than creating parallel systems, follow the full `staffglobal.css` design system for all cards, tables, filters, buttons, tabs, status indicators, charts, modals, spacing, typography, responsive behaviour and theme colours, keep the page laid out economically with headline performance information at the top, analysis in compact panels underneath and detailed entries lower down, retain the existing Overall and personal technician tabs, month picker, Day/Week/Month period filters, anchor date selector, technician selector where permitted and search functionality, add a clear headline efficiency summary showing current efficiency percentage, configured target percentage or target hours where already supported, variance from target and comparison with the previous equivalent period, add compact summary cards for productive/logged hours, available or target hours, allocated hours, overtime and unallocated/non-productive time using the existing source data and without double-counting clocking and manually entered records, add a Today/Week/Month comparison area showing efficiency, productive hours, target/available hours and change against the previous comparable period, add an Efficiency Trend chart across the selected period using the same calculated efficiency data already used by the page, add a Time Breakdown showing productive job time, waiting or idle time where existing clocking/attendance data makes this available, breaks where existing records identify them, overtime and unallocated time, keep categories separate and avoid inventing classifications when the stored data cannot reliably identify them, add a Lost Time Analysis area showing useful causes such as waiting for jobs, jobs exceeding allocated time, breaks, non-productive time and clocking gaps/corrections only when these can be derived from current repository data, add a Jobs Affecting Efficiency area ranking jobs or job requests that are furthest over their allocated time and provide a separate view for jobs completed under allocation, show job number, job/request description, allocated hours, actual logged hours and difference, link job references back to the existing job-card workflow where permissions allow, add Job Category Analysis for categories already represented in existing job status/category/request data such as service, repairs, diagnostics, MOT, warranty and VHC/other, calculate category efficiency from the same allocation and logged-time rules rather than creating independent formulas, add a Daily Target Progress panel showing productive hours achieved, current daily target, remaining hours and percentage complete based on existing technician target, contracted-hours, working-day and weighting logic, add clocking-quality alerts that identify existing-data issues such as unusually long active clockings, missing clock-offs, overlapping records, zero or missing allocations and likely duplicate manual/clocking entries without silently modifying records, keep automatically generated clocking rows clearly identifiable and read-only where the current rules require this, retain the current manual efficiency-entry workflow with job lookup, whole-job or request selection, allocation prefill, logged hours, notes and derived day type, keep all current validation and confirmation behaviour, allow only eligible user-owned manual entries to be edited or deleted unless an existing management permission already grants broader control, add an authorised manager correction workflow only by extending existing management/audit patterns if they already exist in the repository, preserve the original record or change history when a correction is made and never overwrite historical values without traceability, add technician notes/explanations against efficiency periods or affected entries only where a suitable existing notes or audit mechanism can be reused, otherwise keep this as an optional UI concept without introducing an unrelated standalone notes system, add structured efficiency reason indicators for existing causes already represented by data such as parts delays, customer authorisation, diagnostic delays or technical support only if the current schema already records them, add a personal performance comparison allowing a technician to compare their current month against previous periods without exposing other technicians' private performance data, retain the Overall Technician Breakdown for authorised management views showing technician, weight, actual hours, target hours, difference and efficiency percentage and extend it with previous-period efficiency/change where existing data supports this, add compact visual target states such as Below Target, Near Target, On Target and Above Target using shared theme/status styling rather than excessive colour, improve the detailed entries table so each row clearly shows date, job number, description, allocated time, logged time, difference, source, notes and day type with compact allocated-vs-actual visual indication where space allows, keep the table vertically scrollable for large datasets and horizontally usable on smaller screens, improve the existing PDF download so it continues to produce a landscape A4 technician timesheet but also includes technician name, selected period, productive/logged hours, target/available hours, allocated hours, difference, efficiency and any existing manager/technician notes that are already part of the data model, preserve the existing weighted-overall calculations exactly so weighted actual and weighted target continue to multiply each technician's values by their configured weight, make all comparisons and charts consume the same central calculation logic to prevent conflicting figures across the page, handle each asynchronous panel independently with loading, empty, error and stale-data states so a failed chart or analysis section cannot break the core efficiency table, keep realtime subscriptions efficient and refresh only affected data areas where practical, preserve all role checks, audit behaviour and source-of-truth database relationships, maintain desktop, tablet and mobile usability with responsive cards and horizontally scrollable tables where required, and keep the final page focused on helping technicians and permitted managers immediately understand current efficiency, performance against target, where productive time is being lost, which jobs are affecting results, how performance is trending and what data quality issues need attention. deep dive to imapgeo the layout of each card so the work the best with the screen size being used etc

TODO
/stock-catalogue
Update the `/stock-catalogue` page within the existing `App Layout Page Card` so it becomes a compact Parts inventory, stock-control and job-demand workspace while preserving all current Parts access rules, existing catalogue APIs, job-part relationships, job search by job number or registration, workshop part requests, parts pipeline state handling, pre-pick locations, catalogue editing, linked-job behaviour and Add Part to Job workflow, inspect the repository and reuse the existing shared components, database fields, stock calculations, status mappings, role permissions and audit patterns rather than creating duplicate systems, follow the full `staffglobal.css` design system for all cards, tables, search controls, filters, buttons, badges, drawers, modals, spacing, typography, responsive behaviour and theme colours, keep the layout economical with high-value stock information across the top, operational filters and the inventory table as the main workspace, and contextual part information in a compact side panel or modal rather than spreading controls across unnecessary sections, retain the existing Find Job Card functionality and relationships with selected jobs but integrate it cleanly into the improved layout so staff can search by job number or registration, view the matched vehicle/job summary, see all attached parts, filter by populated pipeline stage, inspect workshop part requests and perform the currently authorised status, pre-pick and fitted-quantity actions without changing the existing underlying job-part workflow, add a compact top stock summary showing total active catalogue parts, in-stock count, low-stock count, out-of-stock count, quantity on order, reserved quantity and stock value at cost where the current catalogue pricing data and management permissions support it, calculate and prominently expose Available Stock as On Hand minus Reserved using the same central stock logic everywhere rather than storing another independent stock value, add quick attention filters for All Parts, Needs Attention, Low Stock, Out of Stock, On Order, Reserved and Recently Received using existing catalogue and goods-in data, preserve the current stock-status filter options including Low Stock, Good Stock, High Stock and Back Order and storage-location filtering from A1 through Z10, allow useful secondary filtering by category and supplier where those fields already exist, retain debounced search across part number, description and OEM code but improve result ranking so exact part-number matches appear first, then exact or close OEM matches, then description matches, and keep search responsive for a large catalogue, add a compact Stock Overview showing the distribution of on-hand, reserved, on-order, low-stock, out-of-stock and inactive records based on existing catalogue values, add a Stock Status Breakdown showing On Hand, Reserved, Available, On Order, Back Order and Expected Soon where expected incoming quantities can be derived from existing on-order or Goods In information, add a Demand Overview showing quantities or counts required by open jobs, vehicles waiting for parts, appointments affected and authorised VHC/warranty requirements where existing job, appointment and VHC relationships make those values available, update the primary inventory table so each row can efficiently display part number, description, OEM code, category, storage/bin location, compact On Hand/Reserved/Available stock breakdown, unit cost where permitted, reorder level, status and actions, retain incremental loading or pagination so large catalogues do not render thousands of rows at once, preserve the current part-details workflow but improve the details modal or side drawer to show the part number, description, current stock status, category, supplier, OEM code, storage location, default service zone, unit cost and retail price where permitted, lead-time information where existing data supports it, a Stock Breakdown for On Hand, Reserved, Available, On Order and Back Order, and a Part Demand summary showing how many active jobs require the item, how many are currently waiting and how many near-term appointments may be affected, retain existing pricing calculations for monetary and percentage margin and do not introduce a second margin formula, retain the Linked Jobs area and continue to show appropriate booked or allocated open jobs with links back to their existing job cards, quantity, requirement source and status, add a Recent Stock Movements section using existing Goods In, job allocation, fitting, stock-adjustment and audit data where available so Parts staff can see events such as Goods In +quantity, allocation -quantity, fitted -quantity, returned +quantity and manual adjustment with date, source/job reference and user where recorded, add a controlled Stock Movement action for authorised Parts users that reuses existing stock-adjustment mechanisms and supports legitimate reasons such as stock count correction, damaged, missing, returned, scrapped or relocation without silently changing stock, record all manual stock adjustments through the repository's existing audit approach, add a bin-relocation workflow where supported so a user can move a part between valid storage locations while preserving movement history, add reorder guidance that compares Available Stock with the existing reorder/minimum level and clearly shows how far below the configured level an item is without automatically placing orders, add open-job demand to reorder context so staff can see cases such as `Available 3 • Required by open jobs 5`, add an Expected Soon indicator where quantities are already on order or represented in incomplete Goods In records without treating unconfirmed draft receipts as received stock, add price-change visibility by comparing current catalogue cost with recent completed Goods In values where reliable historical data exists, surface significant changes as informational warnings without automatically modifying retail pricing, retain full Edit Part functionality for the currently supported catalogue fields including name/description, category, supplier, cost price, sell price, quantity in stock, reserved quantity, quantity on order, reorder level, storage location, default service zone and notes, refresh the local inventory result after saving and preserve all existing validation, add duplicate-part detection during creation or editing using normalized part number, OEM code and close description matching while allowing authorised users to review legitimate similar records rather than blocking valid parts without review, add support for displaying superseded or alternative part relationships only if the repository already contains suitable fields or relationships, otherwise do not create a disconnected replacement-parts system, add a compact Top Categories by Value report showing the highest catalogue stock-value categories where cost and category data are sufficiently populated, add a Stock Value Summary for authorised management roles showing cost value, retail value and potential margin using existing catalogue values, add a Dead Stock view identifying catalogue items with no recorded movement or demand for configurable periods such as 90, 180 or 365 days where reliable movement history exists, keep this informational and do not automatically mark or delete stock, add a Recently Added or Recently Received area using catalogue creation and completed Goods In data where available, retain the Add Catalogue Part to Job workflow exactly in terms of searching for a job, selecting quantity of at least one, creating a booked `parts_job_items` requirement and recording the existing Parts workspace source without automatically allocating stock, but improve the modal to show Available Stock and existing demand before confirmation, after saving refresh both the linked-jobs section and selected job's parts information, preserve all current detailed job-part statuses including pending, waiting for authorisation, awaiting stock, on order, pre-picked, stock, allocated, picked, fitted and cancelled and keep pre-pick options connected to the existing service/sales/stairs/no-pick/on-order states, show a clear compact attention warning when the selected job still has pending, awaiting-stock, back-order or other action-required parts, add contextual links between Stock Catalogue and existing Goods In data so an on-order/out-of-stock part can open or inspect relevant incoming receipts where the repository already stores that relationship without merging the two pages or duplicating Goods In functionality, keep the currently dormant delivery-entry/new-part/category implementation dormant unless it is naturally required by an existing visible workflow and do not expose unfinished legacy UI simply because the code exists, handle each asynchronous area independently with loading, empty, error and stale-data states so a failed report, movement panel or job-demand query cannot break core catalogue search and inventory operations, keep queries efficient for the expected catalogue size, avoid repeated fetching of identical inventory/job data, preserve all current permissions, validations, confirmations and audit behaviour, maintain desktop, tablet and mobile usability with responsive summary cards, horizontal table scrolling and a details drawer/modal that remains usable on smaller displays, and keep the finished page focused on allowing Parts staff to immediately see what stock exists, what is actually available after reservations, what is low or out, what is incoming, what jobs require each part, what needs action and how stock has moved while keeping the normal Parts catalogue tightly linked to Goods In and job-card parts through the existing database relationships.deep dive to imapgeo the layout of each card so the work the best with the screen size being used etc

TODO
/goods-in
Update the `/goods-in` page within the existing `App Layout Page Card` so it becomes a fast, compact and reliable Parts receiving workspace while preserving all current role access for Parts, Parts Manager, Service, Service Manager, Workshop Manager and Aftersales management, existing supplier lookup and ledger-link validation, goods-in record creation, duplicate invoice prevention, sequential `GIN-xxxxx` references, document scan behaviour, catalogue search, invoice-line creation, advanced part details, completion workflow, catalogue stock updates and post-completion job assignment including partial quantity splitting, inspect the repository and reuse the existing shared components, APIs, database relationships, catalogue logic, job-part allocation logic, role permissions, confirmation patterns and audit behaviour rather than creating duplicate systems, follow the full `staffglobal.css` design system for all cards, tables, inputs, search controls, buttons, tabs, badges, modals, drawers, spacing, typography, responsive behaviour and theme colours, keep the layout economical with supplier and invoice details compactly grouped at the top, the active part-entry workflow as the primary focus, invoice lines immediately below, and secondary analytics or recent activity in smaller supporting panels rather than allowing dashboard content to dominate the receiving task, retain all current invoice fields including supplier account, supplier name, address, contact, invoice number, delivery note number, invoice date, price level, franchise and internal notes, retain the existing supplier-search modal and continue to prevent selection of suppliers without a linked ledger account, clearly surface the current supplier ledger-link state before allowing parts to be added, retain the lightweight Scan Doc behaviour exactly as implemented and do not describe or redesign it as full OCR unless a reliable OCR system already exists elsewhere in the repository, add a clear Draft Goods In header showing current `GIN` reference, supplier, invoice, delivery note, status, number of invoice lines, total units and current cost value once a record exists, add a fast-entry mode for repetitive receiving so after successfully adding an invoice line the part-entry fields reset appropriately, quantity returns to a sensible default such as 1 where consistent with current behaviour, supplier and invoice context remain untouched and keyboard focus returns to the part-number field, make the part-entry workflow keyboard friendly using existing component patterns so staff can move efficiently through catalogue suggestions, quantity, prices, bin location and Add Part without unnecessary mouse interaction, retain current part fields including part number, quantity, retail price, cost price, bin location, discount code, description and franchise, retain existing catalogue lookup by part number or description and continue prefilling catalogue ID, OEM/main reference, description, bin, retail and cost values from the selected catalogue record, improve catalogue result ranking so exact part-number and OEM matches appear ahead of description matches while keeping search responsive, retain valid A1 through Z10 bin locations and improve ranked bin suggestions using existing usage/storage information where available without introducing invalid locations, retain the Tyre franchise £30 retail/cost relationship and preserve all existing validation around part number and positive quantity, add a live stock preview when an existing catalogue part is selected showing current On Hand, Reserved, Available, On Order and expected stock after receipt using the same central catalogue calculations as `/stock-catalogue`, add a Job Demand preview beside the selected catalogue part showing open jobs currently requiring that part, requested quantities, waiting-vehicle or appointment priority where those relationships already exist, and make this informational until the user deliberately allocates received stock after completion, add duplicate invoice-line detection so entering the same catalogue part or normalized part number multiple times on the same active invoice warns the user and offers to increase the existing line quantity rather than silently creating accidental duplicates while still allowing intentional separate lines when appropriate, add price variance feedback when an existing catalogue part is selected by comparing the received cost and retail values with current catalogue values and recent completed Goods In values where reliable history exists, show percentage and monetary change as an informational warning and never silently overwrite entered pricing before completion, add a margin warning when the new cost against the entered or catalogue retail price produces an unusually low or negative margin using the same existing margin calculation as the catalogue rather than introducing a second formula, clearly label unknown part numbers before adding with a message that completion will create a new active catalogue part, retain all existing advanced part-detail tabs and fields including Global, Dealer, Stock, User Defined, Links, Sales History, Audi, Additional Fields and Online Store but keep Advanced Details collapsed by default so the normal receiving workflow remains quick, retain invoice-line columns for part number, description, retail, cost, surcharge, quantity, total and remove action and extend the line row compactly with catalogue/new-part state, bin and job-demand indicator where useful, keep destructive line removal behind the current confirmation pattern, add invoice-level summary values for total lines, total units, total cost, total retail, potential margin and surcharge totals where existing data supports these calculations, add a compact Receiving Progress indicator showing how much of the current draft has been entered and whether there are unresolved validation issues such as missing supplier ledger account, duplicate invoice, unknown bins, unusual pricing or incomplete line data, retain Refresh and Complete actions and keep Complete disabled or clearly blocked until an existing goods-in record and at least one valid invoice line exist, on completion preserve the current behaviour of marking the goods-in record completed with timestamp, increasing stock for existing catalogue parts, updating supplied cost, retail and storage data where currently supported, creating unknown catalogue parts as active records, linking every goods-in line to its catalogue part and returning successful and failed catalogue update results, improve the completion result into a compact summary showing lines processed, total units received, existing catalogue records updated, new catalogue records created, failed updates and total cost received, do not hide individual failures behind a general success message, after completion retain the existing Add to Job workflow and make the allocation suggestion stage more useful by automatically identifying active jobs already requiring the received catalogue parts using existing job-part, workshop-request, VHC and appointment data, rank suggested jobs using reliable operational context such as vehicle currently waiting, workshop stopped or awaiting stock, appointment today, appointment tomorrow and later bookings where the repository already exposes those states, keep these as suggestions only and never automatically assign received stock without user confirmation, retain search by job number, registration or customer name and the ability to Select All or select individual goods-in lines, retain partial quantity selection, selected versus remaining quantity display, editing and removal before submission, preserve the existing full-quantity assignment behaviour of recording job and customer context and creating the booked job-part item with allocated quantity, preserve the current partial-assignment split behaviour so the original goods-in line keeps the remaining quantity and a matching split line is created for the assigned amount before creating the corresponding booked and allocated job-part item, allow multiple allocation passes before Finish exactly as the existing workflow permits, add a clear Received Quantity versus Assigned Quantity versus Remaining Quantity indicator for each completed goods-in line so Parts staff can immediately see which received items still require shelving or job allocation, where the selected job already has a matching part requirement reuse or reconcile with the repository's current job-part logic rather than blindly creating duplicate requirements, retain all current source and allocation metadata so the resulting requirements remain visible correctly within job cards and `/stock-catalogue`, add a compact Recent Goods In panel showing today's recent records with GIN reference, supplier, invoice, line count, total cost, completion state, user and time using existing records, add unfinished Drafts visibility showing recent draft records that can safely be resumed using the current goods-in data model rather than starting duplicate invoices, add Goods In history search by GIN reference, supplier, invoice number, delivery note, part number and date where existing queries can support this efficiently, add receiving discrepancy handling for Short Supplied, Over Supplied, Damaged and Wrong Item only by reusing or extending existing line notes/status/audit fields if suitable fields already exist, otherwise surface the concept without creating an isolated replacement workflow, add partial-order visibility where existing purchasing/on-order data can identify expected versus received quantities and retain any outstanding amount as awaiting stock rather than treating the whole order as fulfilled, add supplier delivery-performance information such as expected versus received date and days early/late only where the repository already stores an expected delivery date, add compact supplier activity such as recent invoices, spend or delivery performance using existing completed goods-in history but keep this secondary to receiving, add quick actions for Supplier Search, Scan Doc, Catalogue Search, Refresh, Drafts and Complete using existing workflows instead of parallel implementations, keep Goods In tightly connected to the same catalogue displayed by `/stock-catalogue` and the same job-part records used by the job-card workflows, do not mix workshop consumables into this normal Parts Goods In workflow, handle supplier search, catalogue search, invoice lines, stock previews, job-demand suggestions, recent records and completion results with independent loading, empty and error states so a failed secondary panel cannot break the core receiving form, keep queries and rerenders efficient for rapid line entry, preserve all validation, duplicate protection, confirmations, permissions and audit behaviour, maintain desktop, tablet and mobile usability with responsive cards, compact forms and horizontally scrollable invoice tables where needed, and keep the finished page focused on letting Parts staff receive a supplier delivery as quickly as possible, immediately see catalogue and job demand context, catch invoice or pricing mistakes before completion, update stock accurately and allocate newly received parts to waiting jobs with the minimum number of extra steps.deep dive to imapgeo the layout of each card so the work the best with the screen size being used etc



CHECKKKK
One thing to fix on your side
10 of your 22 Vercel env vars are not exposed to the Preview environment — SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_JWT_SECRET, and the seven POSTGRES_* vars (all Production-only). The NEXT_PUBLIC_SUPABASE_* pair is on Preview, so the app will likely render, but anything reading those server-side names will break on Preview only. In Vercel → Settings → Environment Variables, tick Preview on those (pointing at a staging DB if you'd rather previews not touch live data). I didn't change them — that's a data-access decision, and re-scoping them to a live database is your call.

CHECK Drag and drop wihtin next job

cust profile add new job buttonm to uato fill  

CHAT GPT TODO
look into the users not having to click clock on manaully, but this doing it automatically, liek the app knowing when the user is on the job and then finished the job and then on road test etc find out how this can be as automated as possible etc. 

--------------- VHC --------
go through all taps to adjust slight ui desing and make sure everythign works correctly 


------------------ DO NEXT -------------------
CHECK 
SETTING POPUP WITHIN CLOCKING PAGE


TODO
Within "https://hnpsystem.vercel.app/vhc/customer/00076/OSD7Ds_cq_77" for example page remove the notes button, this isn’t visible. Have it so the buttons authorise and decline are set to custglobal.css. Have it so the wheels and tires and brakes and hubs section shows the measurement as well as all of them showing the notes of the concern within the rows. Add a table bottom line in between the rows.  In the top right of this page add a profile button which takes the user to the website/login page or the website/profile page depending if the user is already logged in. Have it so this page is linked to the customers profile page as well so they can open it when they wish. Make sure this page follows the custglobal.CSS page fully. 

TODO
Improve the layout of the website/profile page, I feel like it it’s really long page and not very easy to navigate. Have it so alll /website pages works the best with phone vertical mode. 

TOD with one above maybe. 
Depending on the layout change, have it so the JUMP TO section is hidden in a menu button instead of always visible. 

TODO
chgeck buttons, not following shape and style is this becuase they arent following staffglobal.css settings deep dive into this across all pages

TODO
UPDATE GLOBAL SETTINGS BASED ON THE /staff/website-manager PAGE OUTCOME wiht 




- /website/profile (CHECK)
check global.css style
have it detect if the reg numbers already on the system

UPDATE GLOBAL SETTINGS BASED ON THE /staff/website-manager PAGE OUTCOME wiht dropdown, tab section etc

add a see preview button which when pressed opens a new webbrowser tab and shows what the changes are making from the /staff/website-manager page in real time etc. delete image but still showing fix this as well

- /website (CHECK)
add a link which the staff uers can add delete uploa change etc anything within the /website page from a website page accessed within the "/login" section




--------------------- Cust booking ------------------------
DO it so customer books job thorugh, see if it updates the stuff section. 

have it os the appoinment page is updated within the /website/profile page

i dont see the customer status within the "/job-cards/00076 :: 1.2.1.1.1.2 (jobcard-summary-shell)" section 

CHECK
Have it so within the presnentaion when overview or Business Value popup is highlighting a section, have it so it scrolls the page and makes sure the highlighted section can is within the view of the screen.


CEHCK
customer dev login so it hsows the customers created within the app and shows all the correct details.

CEHCK
monthly pcikers within the app to use global.css style



TODO
look into "Review Media" section after editing photo



TODO DEEP ONE
with camera for concerns, hgave ir link rto vhc photo and vedio section  in catagory of wipers etc

CHECK
Mileage to show within the customer page inside previous job section. 


TODO
npm i next@latest