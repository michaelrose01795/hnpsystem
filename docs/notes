TO DO:
    paul@humphriesandparks.co.uk
        only make this change nothing else
    Make sure pages are all correct and flowing
    Continue flow with job number 57.

DO CHATGPT OVERTIME POPUP PROMPT    



CHECK
Have it so /tracking page has a day filter when the user clicks the calendar opening it opens the jobs for that day. add a cross thught the row if the job has been marked as canceled and add a cycle check box for the "WASH" column, defaul its blank, then when user presses once it turns to a tick, if pressed again then it turns it to a red cross indicating that the wash is not being done (No Wash to be done (have this updte with Job Progress Tracker section))


TODO CODEX
Have it so the section “ /dashboard/valeting :: 7 (dashboard-valeting-auto-content-card-7)” follows accent colour background, and the table uses surface level background with details accent colour. Have it so “ /dashboard/valeting :: 7 (dashboard-valeting-auto-content-card-7)” section shows 4 rows and then a scroll. 









PAGE ACCESS

Service Manager
/dashboard/service



Accounts Manager


/accounts

/accounts/create

/accounts/edit/[accountId]

/accounts/invoices

/accounts/invoices/[invoiceId]

/accounts/reports

/accounts/settings

/accounts/transactions/[accountId]

/accounts/view/[accountId]

/company-accounts

/company-accounts/[accountNumber]

/dashboard/accounts



Admin Manager


/admin/profiles/[user]

/admin/users

/dashboard/admin



Workshop Manager



/job-cards/view

/job-cards/waiting/nextjobs

/messages

/newsfeed

/profile

/tracking

/workshop

/workshop/consumables-tracker

 

Customer


/customer

/customer/messages

/customer/parts

/customer/payments

/customer/vehicles

/customer/vhc

/vhc/customer-preview/[jobNumber]

/vhc/customer-view/[jobNumber]

/vhc/share/[jobNumber]/[linkCode]


Service


/customers

/customers/[customerSlug]



After Sales Manager


/dashboard/after-sales



General Manager


/dashboard/managers




MOT Tester


/dashboard/mot



Painters


/dashboard/painting




Parts Manager


/dashboard/parts


/parts


/parts/create-order

/parts/create-order/[orderNumber]

/parts/deliveries

/parts/deliveries/[deliveryId]

/parts/delivery-planner

/parts/goods-in

/parts/goods-in/[goodsInNumber]

/parts/manager



Valet Service


/dashboard/valeting

/valet



Admin


/dev/status-snapshot

/dev/user-diagnostic



HR Manager


/hr

/hr/attendance

/hr/disciplinary

/hr/employees

/hr/leave

/hr/payroll

/hr/performance

/hr/recruitment

/hr/reports

/hr/settings

/hr/training



Owner


/hr/manager



Techs


/job-cards/myjobs

/job-cards/myjobs/[jobNumber]

/tech/consumables-request

/tech/dashboard

/tech/efficiency

/vhc



Public


/login

/password-reset/reverted

/unauthorized



Parts

s
/stock-catalogue










ADDITIONS 
CHECK - job_writeups, job_writeup_tasks, writeup_rectification_items
have it so when inside writeup tab additional tabs can we written, check these are added to the database.
CHECK - messages, message_threads, message_thread_members
make sure that the unread line is hiden after 30 seconds from thwn the line is in view on the screen, or after the user clicks back off of the chat section/page relaods etc.
CHECK - users
Do a deep dive into the problem where users roles are being removed or swapped with other role names. This is linked to the user database table, where example use 19 role is set to service adviser but this has been changed by code to Service manager instead. Make it so the only way anything within the user database table is changed is within the hr/manager page within the employee tab. Have it so password_hash can be changed using within the login page add a button in small print underneath the login button within that section to reset the password. Have it so when that text button is clicked it opens a popup. This popup auto enters the email address if it’s entered within the login section. The user is then asked to enter a new password. Make sure there are no restrictions on the passwords for example it can be “1” or it can be “iwjfbiri^++!,u74829” for example. Have it so an email it automatically sent to the users email after resetting the password. Add in the email if the email was reset by you then click this link, that link will open up login page to the app which will then give a prompt saying are you sure this wasn’t you as it will revert the password. If the user clicks yes “it wasn’t me” then it reverts the password back to the password used before if “it wasn’t me” clicked then it doesn’t change the entered password. Give me sql code to reset all passwords to “1”. Make sure all emails are sent to “michaelrose01795@icloud.com” instead of the user emails for testing, add a todo which is to make sure it sends to the users email after testing is complete. Have it so within the email it says “[usersname] you have reset your password” as a heading/subject etc.
CHECK - No database tables (email API styling only)
Make all emails sent use a user friendly detailed logoed style. Create an api file which is called email api so all emails use the same style and design when sent from the dms.
CHECK - vhc_checks
have it so if the row is set to decline that it can be edited within the “Parts Identified” tab not just the authorised rows.
CHECK - vhc_checks (display_status column)
if a row gets reset within the summart tab make sure the display_status goes vbabck to amber or red etc. and te hover status front end doesnt say N/A but it hosuld say waiting for customers selection or what ever we have called it
CHECK - invoices, invoice_items, invoice_requests, invoice_request_items
make sure the “Part No	Description	Retail	Qty	Price	VAT	Rate %” tables for each section within the inovice page are always in the same posisition and the same size within each section for example request 1,2,3 etc and VHC request section thoughtout the whole page. if a descritpion is longer then put it onto  a second line within the column.
CHECK - parts_order_cards, parts_order_card_items
parts_job_card_items database table should actaully be parts_order_card_items instead i think. check that it is iused within the parts order numbers cards, if it the case also change the job_id to order_id and make sure this is changed within the code as well. this oculd also be the same with the parts_job_card which should be changed to parts_order_number or along those lines
CHECK - job_notes, floating_notes
Increate the text size within the “New note” section. have it so the whole section uses the same font type, and the title section will use bigger text size as the destipion etc
CHECK - No database tables (CSS/frontend styling only)
Hover button colour theme “N” text is the same colour as the background colour when the button is hover or clicked. have it so the text wihtin the buttons are not the same colour as accent colours when background coloiur changes.
CHECK - vhc_checks (parts_complete, labour_complete, display_status columns)
Fix “Ensure all allocated parts have quantities and pricing.” To give a better explanation and to fix it so when within the parts tab if the parts tab background is set to green eg if the “PARTS ADDED TO JOB” section all of the rows except the removed rows are set as allocated.
CHEKK - vehicles (mileage column), jobs (mileage_at_service column)
Fix within the service history tab the mileage within the row make sure it’s set to the database so it doesn’t say not recorded when it has actually been recorded.
CHECK - No database tables (frontend tab ordering only)
Within the “Customer Requests Contact Scheduling Service History Parts Notes Write Up VHC Warranty Clocking Messages Documents Invoice” section move the notes tab button before the parts tab button.
CHECK - vhc_checks
Fix it so the parts authorised tab shows the authorised items within the vhc_checks database table to show the rows, make sure this saves and whenever the parts authorised tab is opened.
CHECK - No database tables (CSS/frontend styling only)
Have it so the “Job Card #00056 In Progress Retail Created: 19/02/2026, 20:37:17 | Last Updated: 23/02/2026, 23:10:36 Checked in Back” background colour is the same as the “VEHICLE 56231 Save WN69HSU FORD CUSTOMER Zoe Harrington 07851 002991 DECLINED £320.00 VHC Total AUTHORISED £505.99 VHC Total Key location Service showroom Car location Service” sections sit within.
CHECK - No database tables (CSS/frontend styling only)
Have it so the rows within the job-card/view page uses the light and dark colour themed background colour eg level 2 etc.
CHECK - No database tables (CSS/frontend styling only)
have it sow thin the next job page the outstanding jobs row cards don’t use an accent colour eg for light mode white and for dark mode grey for example. Do the same with the user techs cards.
CHECK - No database tables (text content only)
Go thought the whole app to make sure everything is spelt the uk English way
CHECK - No database tables (CSS/frontend styling only)
have it so the “Outstanding Jobs” backgroubd colour is the same colour as the card where the techs cards sit inside, have it so the search bar within the /nextjobs page has a accent boarder colour around it not a full accent colour fill.
CEHCK - news_updates
Within the news feed page make sure there is a bigger gap in between the first row and the add update button so when the hover is used it doesn’t overlap the button.
CHECK - vhc_checks (display_status column), job_clocking, job_requests
Have it so wihtin the jobcards/[jobnuber] page withinthe customer conern tab, have it so the “AUTHORISED VHC” rows at the end where atm it shows “Authorised” change so it matches the “REQUEST 1,2,3 etc” rows example when ticked withn the write up it changes to complete etc, when clocked on to that job line specifically it shows clocked on etc. 




STOCK CHECK PAGE
CHECK - No database tables (CSS/frontend popup behaviour only)

Have it so that all pop ups lock the background scroll or buttons from being clicked so only the items within the popup are active.


CHECk - workshop_consumables, consumable_stock_checks


Within the stock check popup remove the “Select consumables to request and submit them to the workshop manager”, “Create a consumable that everyone in the workshop can see and request. “, “Select items below to include them in a stock check request.”, “Only checked consumables are included in the submission. Only checked consumables are included in the submission. “ text. Have it so the popup fits within the middle of the screen and the popup its self doesn’t go off of the screen view.


CHECk - workshop_consumables

Have it so the “Consumable stock” section only shows the list view after being searched for or if the show list view button is clicked, add the “Show list” button on the same row as the search bar consumables section, have it so the button then shows all of the items but until that is clicked it hides the list view unless the consumable is searched for.


CHECk - consumable_stock_checks, workshop_consumables

Have it so the Stock Check popup within the “0 items selected” so next to the “Submit Stock Check Request” button it has an email button which puts all of the selected items into a email list to the email address “darrell@humphriesandpark.co.uk” set the subject to “Stock Take”


CHECk - workshop_consumables

Have it so when the show list button is clicked and it shows all of the rows it then shows the selected rows at the top of the list view. If they are then unselected then move them back to the original location which follows the alphabetical order starting with numbers.


CHECk - workshop_consumables

Remove the “Add New Consumable” section from within the workshop/consumables-tracker page, keep it within the stock check popup.
CHECk - No database tables (CSS/frontend styling only)
Have it so the text within the workshop/consumables-tracker page follows more of the light and dark colour theme.


CHECk - workshop_consumable_budgets, workshop_consumable_orders


Move the “Adjust Consumables Budget for February 2026” section onto the same row as the “← PreviousFebruary 2026Next →” section have it so this section is removed as it will be shown within the “Budget for February 2026 £0.00” section. Remove the “← PreviousFebruary 2026Next →” section from within the “Monthly Logs” section.


CHECk - No database tables (CSS/frontend styling only)

Remove the “Monitor consumable spend, reorder schedules, and supplier summaries.” Text and make the “Workshop Consumables Tracker” section height smaller.


CHECk - workshop_consumable_orders, workshop_consumables

Within the “Scheduled Consumables” section have it so it will show the “Overdue M gloves 12 Dec 2025 15 Dec 2025 10 Euro £2.39 28.00 Order” rows and then this section “ITEM QTY UNIT TOTAL SUPPLIER DATE M gloves 10 £2.80 £28.00 Euro 12 Dec 2025 M gloves 10 £2.39 £28.00 Euro 12 Dec 2025 M gloves 4 £2.39 £9.56 Euro 09 Dec 2025 M gloves 1 £2.39 £2.39 Euro 09 Dec 2025” is shown within a popup from clicking the row within the Scheduled consumable. 



HR MANAGER
CHECKKK - users, time_records, hr_absences

Fix this “Failed to load profile data Failed to load profile data (status 401) If you continue to see this error, please contact HR to ensure your employee profile has been created.” this problem only happens when using vercel app website, not vercel dev within the terminal, deep dive into fixing this so the vercel release will work the same as the vercel dev.
CHECK - time_records, clocking, job_clocking

CHECK

Make sure the clocking button then works. And check the scheme.sql file that the database is correct. 




TECH DASHBOARD
CHECk - No database tables (CSS/frontend styling only)

within the tech/dashboard page remove the clock in button. Have it so the cards follow light and dark colour theme within this he as well and uses more of the accent colours instead of black buttons. Have it so the text within the page follows more of the light and dark theme and is more visible.


CHECk - jobs, users

Remove the “Welcome back, Cheryl T Tuesday, 24 February 2026” from within the page. Inside the “My Assigned Jobs” section don’t show the total amount in () only show a max of 3 jobs and then add a section saying more jobs available which then the user can click the View all button which takes the user to the job-cards/Myjobs page remove the “View All →” button from the “My Assigned Jobs (4)” section but keep the “View All jobs” button at the bottom of the page.


CHECk - No database tables (frontend only)

Remove the icons and emojis from the whole page.


CHECk - No database tables (frontend routing only)

Have it so the view all jobs goes to the job-cards/Myjobs page. Have it so when the time tracking button is clicked it takes the user to the “/tech/efficiency” page and change the “Request Parts” button to “Request Consumables which will take the user to “/tech consumables-request” page.


CHECk - workshop_consumable_requests

Have it so when any user who has access to the “/tech/consumables-request” page requests a consumable all of the requests wills show within the “Requests” section.


CHECk - No database tables (frontend sidebar config only)

Remove the archive jobs from the sidebar for tech users. Remove the job decision section from the sidebar as well. Remove the Workshop dashboard button from within the General section, but keep it within the My dashboard section.


CHECk - jobs, job_clocking, vhc_checks, job_status_history

Remove the “WORKSHOP WORKSPACE · TUESDAY 24 FEBRUARY Hi Cheryl T, workshop view Live view of technician assignments, queue, and VHC throughput.” Section from the “dashboard/workshop dashboard/workshop” page. Have it so the progress and checking trends are in a 50/50 Split View side by side on one row, have it so both sections are the same width and height. Have it so the next jobs queue shows 3 rows and then scroll, and the same with outstanding VHCs. Remove the status updates section. Have it so the next jobs queue queue and outstanding VHCs are in a 50/50 Split View, they are the same width and height and both have a scroll when there are more than 3 rows, have it so the seizes of the sections are always the same size and they only add a scroll if needed. 


Tracking page
CHECk - key_tracking_events, vehicle_tracking_events, jobs, vehicles

within the key/parking section have it so each card connected to the job number shows the make model and customer name, this can be found for the database table from when creating the job card.


CHECk - key_tracking_events

Have it so under the key location it will just say “service showroom” instead of “keys updated - service showroom”


CHECk - key_tracking_events, vehicle_tracking_events, jobs, vehicles

Have it so the customer name is on the same row as the job number and reg. and keep the make/model/colour in small print below the job number, reg and customer names. On the make/model/colour row move the lasted moved text to this row but have it to the right side of the card and the make/model/colour to the left section of the card.


CHECk - No database tables (frontend visibility only)

If only the key/parking tab is visible example for the tech view then hide the tab section completely. 



MESSAGES
CHECK - messages, message_threads, message_thread_members

Redesign only the thread sidebar and the main message area to feel modern and cleaner, without changing the top bar or any global layout. In the thread section, make the selected thread visually strong with a soft brand-tinted background, a 4px solid brand accent bar on the left, bolder sender name, improved padding, and a small unread badge; refine each thread card with better spacing, lighter preview text, and a subtle hover effect, and upgrade the search bar with a rounded background and icon. In the message section, convert messages into rounded chat bubbles with clear left and right alignment, use a light neutral bubble for other users and a soft brand-tinted bubble for the current user, increase vertical spacing between messages, lighten and reduce timestamp size, add subtle shadows instead of heavy borders, and restyle job numbers as clickable pill badges like “JOB 00056” rather than plain text links, keeping the overall structure intact but improving hierarchy, spacing, and visual clarity.




INVOICE PAGE
CHECK - invoices, invoice_requests, invoice_request_items, vhc_checks

invoice page fix, nearly just have "Ensure all allocated parts have quantities and pricing." create more detail for this easier for users to follow within this seciton.


CHECK - invoices, invoice_requests, invoice_request_items

Have it so within the inovice tab, within the Request 1,2,3 etc rows have it so when the row has a part assigned to it wihti the parts tab it only shows the parts insdie the table which are assigned to that request row or "VHC request" row. If the row doesnt have any parts allocated to it then dont show the "Part No	Description	Retail	Qty	Price	VAT	Rate %" table


CHECK - invoices, invoice_requests, invoice_request_items

make sure that change is for the acutal invocie and proforma invoice.remove "Proforma totals generated from live job data until all invoice prerequisites are complete. " text and section after everything is ready for the inovice to be created, it will show the inovice but this doesnt mean the actual invoice is created until the Create invoice button is pressed. basically the inovice tab will always show "Proforma " section so change the "Proforma totals generated from live job data until all invoice prerequisites are complete. " section text dont remove but change to "Proforma complete ", once the "Create Invoice" button is pressed then that ""Proforma complete " section is hiden. make sure that the "Create Invoice" button isnt visible until the  "Proforma complete"" section is shown






TO DO AFTER THE ONE BELOW
so i have archiving for job numbers, this is everything links to a job is archived and saved as read only intead of full functions. how can i save all of the data within the tables 


CHECK LAST CHANGES FIRST
create a flow for create a job card, start from the /create page and then based on the code used create a story line with "bla bla bla > bla bla bla" like actions to show next page / feaute/ tap/ section. use a code to show which sections are linked to what eg showing the same infomation where else that feaute is used etc. add the databazse table used and if the table can be made better save the data add the database table name and columns withn the story line. have the story line more like bullet points created in a new folder under docs which will be named Jobcard Flow. 



TO DO 
jobs database table work thought as nearly all empty, do a deep dive into finding out why nearly the whole database is emplty and fix any loose ends



VIDEO UPDATE WHEN WORKING
Add overlay to the video which is like a bubble which goes to a fix location within the recording which the user can press during the recording Like ar tiles which give the title an colour of the issue. have it so it alwyas show the front pads and discs measurenmetns as well as all 4 tyres no matter red amber or green. with the ohter conerns always show either red or amber. never green. 




TO BE COMPLETED AT END:

Go through the status of each section and job to make sure status is good and matchs and works well within the database. 




With every page within the top bar to the right of the search add a "?" button which opens a drop down (link to drop down style api) which shows (contact support (this opens messages tab and starts a new message with the uers Lead tech (add this in small print within the section as a todo) which within the text box to send the message shows a / comand which tells the sender the page the user was on and then the user can then input text after either reporting a concern, bug, or a question. The next in the drop down is a "Tutorial" button which gives multiple steps describing what each section and button does inside an overlay type style making the background darker and the section or button the tutorial is telling how to use lighter with the description box like a bubble which is inside the darker section and the bubble is following the light and dark theme, there is also a "Next" and "back" button which flicks through the each section and button to do with that page/the tab selected at the time of the button press with also an "Finish" button which closes the overlay and tutorial section. What woul dbe the best way to implement this? as an api with a file linked to each page and tab which has all of the buttons and section anmes and descriptions inside the api linking or a section within the actual code which reads the code and sees what section does what same with buttons, unless theres another way which is commonly used for tutorials like im describing.

NOTE:


See if database strucure links to all of the code with the src/lib/database/schema/schemaReference.sql also see if any tables are in the src/lib/database/schema/schemaReference.sql and are not being used inside the code and why they arent being used. Make sure that all of the code and inputes saves to the database where required and in the corrected locations add any tables/columns to src/lib/database/schema/addtable.sql



Add any database tables / columns to file location: src/lib/database/schema/addtable.sql, can to see if the table is already inside the file location: src/lib/database/schema/schemaReference.sql file first



npm i next@latest
npm i -g vercel@latest

sudo apt install ripgrep
