

CLAUDE
CHECK
Have it so the messages page works like iPhone messages does on iPhone instead of the current design for bigger screens, have it so it only uses this messages page feature when in iPhone view and other view when in bigger screens view. 

CHECK
Deep dive to override anything stopping this from happening front end as the hcanges havent taken place "Have it so within the tracking page it shows each tracking card in a row instead of being in a 2 column layout. Only make this change with iPhone view. (CHANGE users can click add location and if a job number, reg, or customer already exists it auto fills the information and changes to “Edit existing” instead of “log new”)"

CHECK
Deep dive to override anything stopping this from happening front end as the hcanges havent taken place "Have it so within the “My Assigned Jobs” page the table is in full use when in phone vertical view as atm the contents of the table looks like it’s in the first column."

CHECK
deep dive to make sure this is change happens front end "Have it so “6 techsJob #00056” bubbles follow more of the button style than bubbles, change this front end and find any other sections which use this shape instead of follow the button style (even though it’s not a button)." this is within the clocking tab inside the job-cards/[jobnumber] pge. 