// See customer-messages.js — referenced page-ui was deleted in 92c14681.
import { PageShell, ContentWidth } from "@/components/ui";
import { SectionCard } from "@/components/Section";

export default function CustomerVehiclesMock() {
  return (
    <PageShell>
      <ContentWidth>
        <SectionCard title="Customer · Vehicles">
          Customer vehicles UI is being rebuilt — slide retained for the deck.
        </SectionCard>
      </ContentWidth>
    </PageShell>
  );
}
